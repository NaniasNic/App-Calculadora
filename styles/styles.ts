import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    display: {
        height: 400,
        fontSize: 80,
        color: "#fff",
        textAlign: "right",
        padding: 15,
        backgroundColor: "#1c191c"
    }
})